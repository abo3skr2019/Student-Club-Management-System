<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تحديث الملف الشخصي</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">

    <!-- Custom CSS -->
    <link href="/css/update-profile.css" rel="stylesheet">
    <link href="/css/header.css" rel="stylesheet">
    <link href="/css/footer.css" rel="stylesheet">
</head>
<body>

<%- include('partials/header', { currentPage: 'update-profile' }) %>


<div class="container">
    <div class="login-container">
        <h1>تحديث الملف الشخصي</h1>
        <p>حياك الله! <span class="emoji">👋</span> اسمك مضبوط؟ إذا تبيه كذا سو تخطي. وإذا فيه شيء تبي تعدله، عدله. وخلنا نكمل <span class="emoji">🚀</span></p>

        <% if (!user) { %>
            <p>يرجى <a href="/login">تسجيل الدخول</a> لتحديث ملفك الشخصي.</p>
        <% } else { %>
            <form action="/update-profile" method="POST">
                <label for="firstName" class="form-label">الاسم الأول:</label>
                <input type="text" id="firstName" name="firstName" class="form-control" value="<%= user.firstName %>">

                <label for="lastName" class="form-label">الاسم الأخير:</label>
                <input type="text" id="lastName" name="lastName" class="form-control" value="<%= user.lastName %>">

                <div class="d-flex justify-content-between mt-4">
                    <button type="submit" class="btn btn-primary">تحديث</button>
                    <a href="/profile" class="btn btn-secondary">تخطي</a>
                </div>
            </form>
        <% } %>
    </div>
</div>

<%- include('partials/footer') %>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
