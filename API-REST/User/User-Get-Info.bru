meta {
  name: Get-User-Info
  type: http
  seq: 1
}

get {
  url: http://localhost:5000/profile?fields={{selectedFields}}&include={{includeRelations}}
  body: none
  auth: none
}

params:query {
  fields: {{selectedFields}}
  include: {{includeRelations}}
}

headers {
  x-user-id: {{userId}}
}

vars:pre-request {
  userId: 6
  selectedFields: "firstName,lastName,uuid,uniId,globalRole,profileImage,displayName,email"
  includeRelations: "eventRegistration"
}

docs {
  # User Information API
  
  This endpoint retrieves user information with configurable field selection and relation inclusion.
  
  ## Authentication
  - Requires a valid user ID in the x-user-id header
  - Note: This is a development/testing approach only - production uses Token-based authentication
  
  ## Query Parameters
  - `fields`: Comma-separated list of user fields to include in the response
    - Available fields: uuid, uniId, nationalId, phoneNumber, displayName, firstName, lastName, email, profileImage, globalRole
  - `include`: Comma-separated list of relations to include with the user
    - Available relations: eventRegistration, clubMemberships, clubsSupervised
  
  ## Example
  ```
  GET /profile?fields=firstName,lastName,email&include=eventRegistration
  x-user-id: 6
  ```
  
  ## Response
  - `200 OK` on success with the requested user data
  - `401 Unauthorized` if user is not authenticated
  - `404 Not Found` if user doesn't exist
  - `500 Internal Server Error` for server issues
  
  ### 200 Response Example
  ```json
  {
    "user": {
      "firstName": "Regular",
      "lastName": "User",
      "uniId": "556098882",
      "globalRole": "user",
      "profileImage": "https://ui-avatars.com/api/?name=Regular%20User",
      "displayName": "Regular User",
      "email": "556098882@university.edu.sa",
      "eventRegistration": [
        {
          "id": 3,
          "userId": 6,
          "eventId": 1,
          "status": "accepted",
          "createdBy": 6,
          "updatedBy": 6,
          "isArchived": false,
          "archivedAt": null,
          "createdAt": "2025-05-20T09:27:06.371Z",
          "updatedAt": "2025-05-20T09:27:06.371Z"
        }
      ]
    }
  }
  ```
}
